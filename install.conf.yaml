- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.gitconfig: .gitconfig
    ~/.irbrc: .irbrc
    ~/.zshrc: .zshrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]
